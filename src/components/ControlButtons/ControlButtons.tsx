import React from "react";
import { useControlButtonsStyle } from "./style";

const ControlButtons = () => {
  const { ControlButtonsSC } = useControlButtonsStyle();

  return <ControlButtonsSC>1</ControlButtonsSC>;
};

export default ControlButtons;
