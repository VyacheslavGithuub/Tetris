import styled from 'styled-components';

export const StyledStartButton = styled.button`
  position: absolute;
  top: calc(50% - 35px);
  left: calc(50% - 35px);

  z-index: 10;
  width: 70px;
  height: 70px;
  border-radius: 100%;
  color: white;
  border: none;
  cursor: pointer;
`;
